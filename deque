#include <iostream>
#include <deque>
#include <string>
using namespace std;

int main() {
    
    deque<pair<int,string>> a(2, make_pair(-1, string("NULL")));  

    deque<pair<int,string>> b;         

    a.push_front(make_pair(1, "Mot")); 
    a.push_front(make_pair(2, "Hai")); 
    b = a;   

    while(!b.empty()) {
        pair<int,string> v = b.front();   
        b.pop_front();                   
        cout << "(" << v.first << "," << v.second << ")";
    }

    return 0;
}
